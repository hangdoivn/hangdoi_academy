(()=>{const form=document.querySelector('.abh-search');const input=document.getElementById('abh-search-input');const clear=document.getElementById('abh-search-clear');const status=document.getElementById('abh-search-status');const empty=document.getElementById('abh-no-results');const cards=[...document.querySelectorAll('.abh-article-card')];if(!form||!input)return;const normalize=s=>(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim();const apply=()=>{const q=normalize(input.value);let visible=0;cards.forEach(card=>{const show=!q||normalize(card.dataset.search).includes(q);card.hidden=!show;if(show)visible+=1});clear.hidden=!q;empty.hidden=visible!==0;status.textContent=q?`${visible} bài phù hợp với “${input.value.trim()}”.`:`Tìm trong ${cards.length} bài đã xuất bản.`};form.addEventListener('submit',event=>{event.preventDefault();apply()});input.addEventListener('input',apply);clear.addEventListener('click',()=>{input.value='';apply();input.focus()});})();
